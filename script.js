function matrixDisplay() {
  var isClicked=true
  var matrixTitle = document.querySelector(".matrix");
  if (isClicked){
    matrixTitle.style.display="block"
  }
  else{
    matrixTitle.style.display="none"
  }
}
